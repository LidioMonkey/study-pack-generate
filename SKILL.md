---
name: "考试复习包"
description: "把老师的 PPT、PDF、讲义、课堂笔记和往年题一键整理成复习包：自动划重点、总结题型考点、生成练习题和闪卡、做出能打印的考前速记页，再按考试日期排好每天的复习计划。适用于期末考试、期末周突击、补考、考研、自考和证书考试。"
version: "1.0.1"
---

# 考试复习包

## 核心目标

把用户提供的课程资料整理成可复习、可自测、可按天执行的期末复习包。核心链路：

```text
资料盘点 -> 覆盖图 -> 复习包 -> 自测材料 -> 复习计划
```

## 参考文件

- 处理课件、PDF、Word、图片和往年题前，读取 `references/intake-and-coverage.md`
- 生成复习包正文前，读取 `references/study-pack-output.md`
- 生成闪卡、题库、限时模拟前，读取 `references/flashcards-and-questions.md`
- 安排每日复习、提醒和冲刺计划前，读取 `references/review-plan.md`
- 需要稳定输出文档时，使用 `assets/study-pack-template.md` 或 `assets/review-plan-template.md`
- 生成视觉版速记卡或 HTML 复习包时，以 `assets/study-pack.html` 为模板改内容，不要从零写样式
- 需要校验结构化复习包 JSON 时，运行 `node scripts/validate-study-pack.mjs <json-file>`

## 默认交付物

用户没有指定格式时，默认交付以下内容：

1. 资料清单与覆盖图。
2. 按章节组织的复习包。
3. 题型与考点映射表。
4. 可直接自测的练习题和答案解析。
5. 原子化闪卡表。
6. 按考试日期或剩余天数安排的复习计划。
7. 考前一页速记。

## 何时使用

使用本技能：

- 用户上传老师 PPT、PDF、Word、课堂笔记、教材节选、往年题、样卷或考试范围。
- 用户说“帮我整理期末复习资料”“总结题型”“生成题库/闪卡”“做考前速记”“安排复习计划”。
- 用户提供考试日期、剩余天数、每天可用时间，要求冲刺复习。

不要使用本技能：

- 用户只要求普通文件转换、合并、排版或 OCR，不需要复习内容。
- 用户要求作弊小抄、隐藏答案、替考、绕过考试监管。
- 用户没有提供任何课程资料，却要求对非公开课程给出确定的老师出题重点。

## 核心流程

1. 先读取并盘点资料，不要直接总结。
2. 生成资料覆盖图，标明已覆盖、缺失和无法识别的部分。
3. 把“资料原文重点”和“推断出的复习建议”分开写。
4. 按章节生成复习包正文，重点必须尽量带资料位置。
5. 从复习包生成题型总结、闪卡和练习题。
6. 根据考试日期、剩余天数和每天可用时间安排复习计划。
7. 用户要求文件、表格、提醒或图示时，使用当前环境已有能力完成，不指定具体工具名。
8. 交付结构化文件前，优先用校验脚本检查字段完整性；缺字段时先补齐再交付。

## 交付节奏

1. 第一轮回复就要有可见成果：先交付资料清单、覆盖图和“必会重点速览”，不要静默读完全部资料才开口。
2. 每读完一份资料就汇报一行盘点（文件名、覆盖章节、提取到什么），让用户全程看到进展；禁止连续多轮工具调用之间没有任何文字输出。
3. 速览交付后继续生成完整复习包，不需要等用户确认；用户中途有新指令时优先响应。
4. 资料很多时按章节分批交付，每批都是当下就能用来复习的完整片段。
5. 缺少考试日期、每天可用时间等信息时不要阻塞：先按默认假设产出并标明假设，再请用户修正。

## 产物文件

交付完整复习包或用户要求文件时：

- 同一门课程的产物统一放在工作目录 `复习包-<课程名>/` 下，不散落在根目录。
- 默认产出：`复习包.md`、`复习计划.md`、`闪卡.csv`、`考前速记.html`；用户要求 Word/PPT/Excel 等格式时用当前环境已有能力转换。
- `考前速记.html` 是单文件视觉版速记卡：基于 `assets/study-pack.html` 模板生成，浏览器打开即用、可打印成 A4；写法规范见 `references/study-pack-output.md` 的「HTML 产物规范」。
- 用户要求“好看的/可视化/能打印”的完整复习包时，同样基于该模板生成 `复习包.html`。
- 交付时在对话里列出生成的文件路径和各自用途。

## 与错题复盘联动

- 用户做练习题答错时，不只给答案：按“第一处关键错误 -> 错因 -> 正确思路 -> 同类变式”复盘。
- 提示用户把做错的题沉淀进错题本（错题复盘助手负责维护工作目录 `错题本/`）。
- 工作目录已有 `错题本/` 时，复习计划要读取它，为高频错因和薄弱章节加权安排时间。

## 结构化输出字段

需要输出 JSON 或可被后续任务继续处理的数据时，使用这些字段：

```text
materialInventory
coverageMap
chapterKnowledgeTree
priorityFocus
confusingPairs
glossary
formulasAndFigures
questionTypeMap
practiceQuestions
flashcards
reviewPlan
quickReviewSheet
```

## 学术诚信

可以帮助整理、理解、练习、复盘和规划。不得帮助作弊、替考、伪造学习记录、绕过监考或生成隐藏答案材料。